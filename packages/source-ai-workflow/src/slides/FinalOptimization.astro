---
import Slide from "@presentation/components/slide/Slide.astro";
import { highlightCode } from "@presentation/utils/shiki-helper";

interface Props {
  order?: number;
}

const { order } = Astro.props;

const skillCreatorCode = `# Posledný update

## Pred optimalizáciou:
guidelines/ → commands/ → skills/
(3 vrstvy, veľa commandov)

## Po optimalizácii:
guidelines/ → skills/
(guidelines presunúté priamo do skillov)

## Skill Creator od Anthropic:
# Preformátoval všetky skilly
# do aktuálneho štandardu

# Výsledok:
# ✓ Zredukovaný počet slash commandov
# ✓ Skills obsahujú všetko potrebné
# ✓ Formát zodpovedá best practices
# ✓ Lepšia AI performance`;

const highlightedCode = await highlightCode(skillCreatorCode, "markdown");
---

<Slide title="Finálna optimalizácia" desc="Skills ako single layer" order={order}>
  <div slot="left">
    <p>
      Guidelines, ktoré nepatrili pod UI, presunúté
      <strong>priamo do skillov</strong>.
    </p>
    <br />
    <p>
      <strong>Zredukovaný počet</strong> slash commandov.
    </p>
    <br />
    <p>
      Všetky skilly prehnané cez <strong>Skill Creator</strong>
      od Anthropic – preformátované do aktuálneho štandardu.
    </p>
  </div>
  <div slot="right" set:html={highlightedCode} />
</Slide>
