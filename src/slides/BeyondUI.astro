---
import Slide from "../components/slide/Slide.astro";
import { highlightCode } from "../utils/shiki-helper";

interface Props {
  order?: number;
}

const { order } = Astro.props;

const expandedGuidelines = `# Rozšírenie na celý framework

docs/framework/guidelines/
├── project-structure/
│   └── folder-structure.md
├── routing/
│   └── routing-guidelines.md
├── i18n/
│   └── i18n-structure.md
├── forms/
│   ├── form-composition.md
│   └── form-validation.md     # Zod + RHF
├── state/
│   └── redux-guidelines.md
├── api/
│   ├── rtk-query.md
│   └── domain-api-exposure.md
├── microfrontends/
│   └── mfe-packages.md
├── hooks/
│   └── public-hooks.md
└── neuron-ui/
    └── ...                    # 80+ components`;

const highlightedCode = await highlightCode(expandedGuidelines, "markdown");
---

<Slide title="Za hranice UI" desc="Celý frontend framework" order={order}>
  <div slot="left">
    <p>
      Nemusíme to používať <strong>len na design systém</strong>.
    </p>
    <br />
    <p>
      Vznikli guidelines na:
    </p>
    <ul>
      <li><span>Project folder structure & routing</span></li>
      <li><span>i18n štruktúra & form composition</span></li>
      <li><span>Redux state management</span></li>
      <li><span>RTK Query & API z domény</span></li>
      <li><span>Microfrontend balíky & public hooky</span></li>
    </ul>
  </div>
  <div slot="right" set:html={highlightedCode} />
</Slide>
