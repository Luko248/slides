---
import Slide from "../components/slide/Slide.astro";
import { highlightCode } from "../utils/shiki-helper";

interface Props {
  order?: number;
}

const { order } = Astro.props;

const templateFlow = `# INPUT: templates/commands/ui-button.template.md
---
description: Neuron UI button component usage
---
Your task is to integrate NeuronButton...

# OUTPUT → Claude Code (.claude/commands/ui-button.md)
---
description: Neuron UI button component usage
---
Your task is to integrate NeuronButton...

# OUTPUT → Copilot (.github/prompts/ui-button.prompt.md)
---
agent: agent
---
Your task is to integrate NeuronButton...

# OUTPUT → Windsurf (.windsurf/workflows/ui-button.md)
---
description: Neuron UI button component usage
auto_execution_mode: 3
---
Your task is to integrate NeuronButton...`;

const highlightedCode = await highlightCode(templateFlow, "markdown");
---

<Slide title="Template → Output" desc="Jeden vstup, tri výstupy" order={order}>
  <div slot="left">
    <p>
      Rovnaký <strong>content body</strong>, rôzny frontmatter a file extension
      per adaptér.
    </p>
    <br />
    <p>
      <code>{"{{description}}"}</code> → injektuje popis z metadát
    </p>
    <br />
    <p>
      <code>{"{{commands}}"}</code> → interpoluje zoznam commandov
    </p>
  </div>
  <div slot="right" set:html={highlightedCode} />
</Slide>
