---
import Slide from "../components/slide/Slide.astro";
import { highlightCode } from "../utils/shiki-helper";

interface Props {
  order?: number;
}

const { order } = Astro.props;

const workflowExample = `---
description: Create Neuron Button component
---

Your task: integrate NeuronButton component.

## Steps:
1. Read @docs/neuron-ui/buttons/button-guidelines.md
2. Identify the correct variant from the prompt
3. Import from '@neuron/design-system'
4. Apply i18n for button labels
5. Follow spacing rules from guidelines

## Available variants:
- primary | secondary | tertiary | ghost
- small | medium | large

## Example:
\`\`\`tsx
import { NeuronButton } from '@neuron/design-system';

<NeuronButton variant="primary" size="medium">
  {t('common.submit')}
</NeuronButton>
\`\`\``;

const highlightedCode = await highlightCode(workflowExample, "markdown");
---

<Slide title="Slash Commands" desc="Windsurf workflows & Claude commands" order={order}>
  <div slot="left">
    <p>
      Windsurf oznámil <strong>workflows</strong> – ekvivalent
      custom slash commandov.
    </p>
    <br />
    <p>
      <code>/button primary variant</code> → agent to spravil.
    </p>
    <br />
    <p>
      Nevytvárame nové guidelines, ale <strong>abstrakciu</strong>
      nad existujúcimi.
    </p>
    <br />
    <p>
      Viac ako <strong>100 markdown súborov</strong> odkazujúcich
      na konkrétne guidelines.
    </p>
  </div>
  <div slot="right" set:html={highlightedCode} />
</Slide>
